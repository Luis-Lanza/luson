// Package handlers contains the HTTP request handlers for the API endpoints.
// Each handler implements a specific use case by coordinating with the
// application services and returning appropriate HTTP responses.
package handlers
